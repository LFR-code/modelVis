% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_sim.R
\name{mv_sim_dashboard}
\alias{mv_sim_dashboard}
\title{Generate interactive simulation dashboard}
\usage{
mv_sim_dashboard(
  blob,
  label = NULL,
  probs = c(0.025, 0.5, 0.975),
  perf_stats = NULL,
  output_file = NULL
)
}
\arguments{
\item{blob}{An MS3 simulation blob (from
\code{runMS3()} / \code{load()}).}

\item{label}{Character label for this scenario.
If NULL, constructed from scenario + MP name.}

\item{probs}{Numeric vector of length 3: lower, median,
upper quantile probabilities for simulation envelopes.
Default \code{c(0.025, 0.5, 0.975)}.}

\item{perf_stats}{Optional data frame of pre-computed performance
statistics (one row) passed through to \code{extract.ms3Blob}.}

\item{output_file}{Path for the output HTML file.
If NULL, opens an interactive viewer.}
}
\value{
Invisibly returns the path to the rendered
HTML file (if \code{output_file} is specified) or
NULL (if interactive viewer is used).
}
\description{
Extracts standardised output from an MS3 simulation
blob and renders an interactive HTML flexdashboard
showing simulation envelopes, example replicates,
and performance summaries.
}
